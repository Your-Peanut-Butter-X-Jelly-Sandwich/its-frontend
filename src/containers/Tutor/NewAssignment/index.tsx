import React from "react";

const NewAssignmentContainer: React.FC = () => {
  return (
    <div>
      Create Assignment, each assignment can contain multiple questions Upon
      visiting the page, there will be a list *initially empty* showing the
      current list of qns under this assignment
      <span>button to add question</span>
    </div>
  );
};

export default NewAssignmentContainer;
