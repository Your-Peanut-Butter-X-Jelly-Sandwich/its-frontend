import React from "react";

const TutorDashboardContainer: React.FC = () => {
  return (
    <div>
      This is the tutor landing page. *After logged in as tutor, this is the
      page we redirect the users to. This page should contain functions for
      users to create new assignments and see past created assignments.
    </div>
  );
};

export default TutorDashboardContainer;
